# ─── Creator City · AI 工作台环境变量示例 ────────────────────────────────────
# 复制本文件为 .env.local，按需填写，然后重启开发服务器。
# .env.local 已在 .gitignore 中，不会提交到代码仓库。

# ─── 选择 AI Provider ────────────────────────────────────────────────────────
#
# mock  → 快速模拟，无需 API Key，适合本地开发和 UI 调试（默认）
# real  → 真实大模型，需配置下方 API Key
# 不填  → 自动检测：有 Key 则用 real，否则用 mock
#
AI_PROVIDER=mock

# ─── Anthropic · Claude（优先级高于 OpenAI）─────────────────────────────────
#
# 获取 Key：https://console.anthropic.com/
# 默认模型：claude-opus-4-7
#
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# ANTHROPIC_MODEL=claude-opus-4-7

# ─── OpenAI（Anthropic 未配置时使用）────────────────────────────────────────
#
# 获取 Key：https://platform.openai.com/api-keys
# 默认模型：gpt-4o
#
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# OPENAI_MODEL=gpt-4o

# ─── 最小可用示例 ─────────────────────────────────────────────────────────────
#
# 场景 A · 仅用 mock（无需任何 Key）：
#   AI_PROVIDER=mock
#
# 场景 B · 接入 Claude：
#   AI_PROVIDER=real
#   ANTHROPIC_API_KEY=sk-ant-xxxxxxxx
#
# 场景 C · 接入 OpenAI：
#   AI_PROVIDER=real
#   OPENAI_API_KEY=sk-xxxxxxxx
#
# 场景 D · 自动模式（有 Key 用 real，无 Key 用 mock，real 失败自动 fallback）：
#   ANTHROPIC_API_KEY=sk-ant-xxxxxxxx
#   （不设置 AI_PROVIDER）
